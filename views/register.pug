html
  head
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous")
  script.
      $(document).ready(function () {
        $('#submit').click(function(event) {
          if($('#email').val().trim().length === 0 || $('#password').val().trim().length === 0 || $('#name').val().trim().length === 0) {
            event.preventDefault();
            $('#error-message').html("Your username or password is empty.");
          }
        });
      });

body
  page-header
  h1 BA 
  if user
  nav(class="navbar navbar-light bg-faded")
    ul.nav.nav-tabs
      li(role='presentation')
        a(href='/') Homepage
      li.active(role='presentation')
        a(href='/register') Register
      li(role='presentation')
        a(href='/login') Login
      li(role='presentation')
        a(href='/users/') Profile
      li(role='presentation')
        a(href='/createpost') Create post
      li(role='presentation')
        a(href='/posts') See own posts
      li(role='presentation')
        a(href='/allposts') See all posts
      li(role='presentation')
        a(href='/comments') Leave comment

      li(role='presentation')
        a(href='/logout') Logout
    
    .container
      .row
        .col-xs-6.offset-xs-3
          h1.text-xl-center Register
          hr
          form(action="create" method='post' class= "form" name= "message")
            .form-group
              label Name:
              input(type="text" name='name' autofocus required placeholder="Kerem Bakir" autocomplete="off" class="form-control")
            .form-group
              label Email:
              input(type="text" name='email' required placeholder="kerembakir@gmail.com" autocomplete="off" class="form-control")
            .form-group
              label Password:
              input(type="text" name='password' required placeholder="blablabla" autocomplete="off" class="form-control")
            .form-group
              button(type="submit" class="btn btn-primary btn-lg btn-") Submit
          









    script(src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.12.0.min.js")



    //- else toevoegen
